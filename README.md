# Mutugi Interiors LTD

Full-stack portfolio site for Mutugi Interiors LTD.

## Run locally

```bash
npm start
```

Open http://localhost:3000.

The contact form submits to `POST /api/enquiries` and stores validated enquiries locally in `data/enquiries.json`. The `GET /api/health` endpoint reports server status.
